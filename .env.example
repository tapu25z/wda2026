# Copy thành .env ở thư mục gốc (cùng cấp docker-compose.yml) nếu cần đổi cổng publish.
# Lỗi "Bind for 0.0.0.0:6379 failed: port is already allocated" → bật dòng dưới:
# REDIS_PUBLISH_PORT=6380
#
# Khi đổi cổng, trong apps/api/.env đặt REDIS_PORT=6380 (trùng cổng host bạn map).
